CREATE TABLE IF NOT EXISTS `az_article` (
    `id` INTEGER PRIMARY KEY,
    `subTitle` TEXT,
    `title` TEXT,
    `cover` INTEGER,
    `tabs` TEXT,
    `intro` TEXT,
    `content` TEXT,
    `author` TEXT,
    `createDate` TEXT,
    `updateDate` TEXT,
    `pubDate` TEXT,
    `deleteDate` TEXT
)