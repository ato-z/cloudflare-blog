CREATE TABLE IF NOT EXISTS `az_article` (
    `id` INTEGER PRIMARY KEY,
    `subTitle` TEXT,
    `title` TEXT,
    `cover` INTEGER,
    `tags` TEXT,
    `intro` TEXT,
    `content` TEXT,
    `createDate` TEXT,
    `updateDate` TEXT,
    `pubDate` TEXT,
    `deleteDate` TEXT
)